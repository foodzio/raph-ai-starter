# Monorepo Environment Configuration
# This file provides an overview of all environment variables needed across the project.
# For service-specific variables, see:
# - frontend/.env.example (Next.js frontend variables)
# - backend/.env.example (Node.js backend variables)

# =============================================================================
# SHARED VARIABLES (needed by both frontend and backend)
# =============================================================================

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-at-least-32-chars"

# =============================================================================
# FRONTEND VARIABLES (in frontend/.env.example)
# =============================================================================
# NEXT_PUBLIC_SUPABASE_URL
# NEXT_PUBLIC_SUPABASE_ANON_KEY

# =============================================================================
# BACKEND VARIABLES (in backend/.env.example)
# =============================================================================
# DATABASE_URL, DIRECT_URL, SUPABASE_SERVICE_ROLE_KEY
# EMAIL_*, RESEND_API_KEY, AWS_*, AI API KEYS
# INNGEST_EVENT_KEY, PROXYCURL_API_KEY, etc.

# Instructions:
# 1. Copy this file to .env in the root directory
# 2. Copy frontend/.env.example to frontend/.env
# 3. Copy backend/.env.example to backend/.env
# 4. Fill in your actual values in all three .env files
